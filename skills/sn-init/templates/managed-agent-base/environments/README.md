# environments/

Per-environment configuration consumed by `ant`. Each file declares `vars` and `secrets` references.

`default.yaml` is the baseline. Add `staging.yaml`, `prod.yaml`, etc. as needed.
