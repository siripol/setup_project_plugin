# skills/

Project-local Skills. Each skill lives in its own directory with a `SKILL.md` file (frontmatter + body).

Upload via the Skills API or mount via `ant skills apply`. Reference them from `agents/main.yaml` `skills:` block.

See `example-skill/` for the minimal shape.
