# Security

## Threat model

(Likely threats + assumptions about attackers.)

## Secret hygiene

- Secrets only via `environments/` + Vault refs.
- Never paste API keys into prompts.

## Data handling

(PII boundaries, retention, sanitization.)
