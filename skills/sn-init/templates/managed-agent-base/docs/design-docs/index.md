# Design docs

Architecture, invariants, decisions. Authored — not auto-generated.

## Architecture

(High-level component diagram + data flow.)

## Invariants

(Architectural rules. Each should have a matching test in `.harness/invariants/`.)

## Decisions

(ADR-style records of significant choices.)
