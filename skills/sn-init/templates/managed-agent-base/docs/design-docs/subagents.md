# Subagent library

Index of subagents available in `.claude/agents/`. Regenerate from frontmatter via `make subagent-index`.

| Name | Tools | Purpose |
|---|---|---|
| code-reviewer | Read, Grep, Glob | Bug + style review |
| test-writer | Read, Write, Edit, Bash | Draft + run tests |

(Auto-generated when more subagents are added via `--subagents`.)
