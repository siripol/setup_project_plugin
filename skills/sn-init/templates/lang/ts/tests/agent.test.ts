import { describe, it, expect } from "vitest";

describe("agent module", () => {
  it("compiles + smoke-imports", async () => {
    // Just verifies the source can be statically parsed.
    expect(true).toBe(true);
  });
});
