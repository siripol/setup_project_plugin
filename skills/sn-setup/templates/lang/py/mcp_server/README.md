# mcp_server/ (Python)

`fastmcp`-based MCP server stub. Add tools by decorating functions with `@mcp.tool()`.

Run via `make mcp-server` (calls `uv run mcp_server/main.py`). Registered in `mcp/mcp.json`.
