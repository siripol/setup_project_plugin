# .claude/commands/

Local slash commands. Each `*.md` file becomes a `/<name>` command. Frontmatter:

```yaml
---
name: my-command
description: What this command does
---
```

Per Anthropic docs, this is the legacy slash-command format. Prefer skills/ for new commands.
