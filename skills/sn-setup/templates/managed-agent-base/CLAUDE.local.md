---
local_only: true
gitignore: true
last_updated: ${date}
---

# ${name} — local Claude memory

Local-only notes layered on top of `CLAUDE.md`. Gitignored. Auto-loaded.

## Why this file

Per-developer or per-machine memory. Use it for:
- Machine-specific paths, ports, DB URLs.
- Current focus / WIP.
- Fresh project conventions not yet promoted to `CLAUDE.md`.

## Local environment

- (add machine-specific paths, ports, DB urls here)

## Per-developer notes

- (your current focus, open questions, WIP)

## New common claude data

- (running log; promote stable items to root `CLAUDE.md`)

## Recent additions

- ${date} — initial scaffold from sn-setup
